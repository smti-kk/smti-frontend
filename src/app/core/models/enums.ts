export enum MailType {
  UPC = 'upc',
  MAIL_CONNECTION = 'post',
}

export enum SignalType {
  ATV = 1,
  CTV = 2,
}

export enum Quality {
  GOOD = 'good',
  NORMAL = 'normal',
  ABSENT = 'absent',
}
