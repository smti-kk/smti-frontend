export enum MailType {
  UPC = 'upc',
  MAIL_CONNECTION = 'post'
}

export enum TrunkChannelType {
  UNDEFINED,
  VOLS = 3,
  SATELLITE = 4,
  COPPER_CABLE = 5,
  RADIO_CHANEl = 6
}

export enum SignalType {
  ATV = 1,
  CTV = 2
}

export enum Quality {
  GOOD = 'good',
  NORMAL = 'normal',
  ABSENT = 'absent'
}
